namespace TheSlimeKing.Inventory
{
    public enum EquipmentType
    {
        None,
        Topo,    // Equipamento superior (cabeça, peito)
        Meio,    // Equipamento do meio (cintura, mãos)
        Baixo    // Equipamento inferior (pernas, pés)
    }
}
